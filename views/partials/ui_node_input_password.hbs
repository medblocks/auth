<fieldset>
        <span>{{getTitle .}}</span>
    <label>
        <input name="{{attributes.name}}" type="{{attributes.type}}" value="{{attributes.value}}" placeholder="{{getTitle .}}" {{#if attributes.disabled}}disabled{{/if}}>
        {{> ui_node_input_password_toggle }}
    </label>
    {{> messages }}
</fieldset>
