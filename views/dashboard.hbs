<div class="content">
    {{> header}}
    <div class="container">
        <h2 class="greeting">Welcome back, <span class="user-identifier">{{session.identity.traits.name.first}} {{session.identity.traits.name.last}}</span>!</h2>
        <h3>This example app is secure by default!</h3>
        <p>
            Hello, nice to have you! You signed up with this data:
        </p>
        <pre><code>{{jsonPretty session.identity.traits}}</code></pre>

        {{!-- <p>which a ExpressJS middleware checks for validity and payload. </p> --}}
            <p>The decoded ORY Kratos Session payload is as follows:</p>
        <pre><code>Name : {{session.identity.traits.name.first}} {{session.identity.traits.name.last}}
Email : {{session.identity.traits.email}}
Verified : {{session.identity.expires_at}}
{{jsonPretty session}}
        </code></pre>
    </div>
</div>
